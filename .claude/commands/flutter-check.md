Run the following quality checks on this Flutter project in order and report any issues found:

1. `dart format --output=none --set-exit-if-changed .` — check formatting (report unformatted files, do not modify)
2. `flutter analyze --fatal-infos` — static analysis
3. `flutter test` — run the full test suite

For each step, report: pass or fail, and list specific errors with file paths and line numbers.

After reporting, fix any issues found (formatting, analysis warnings, failing tests) and re-run the checks to confirm all pass. Do not stop until all three checks pass with zero issues.
